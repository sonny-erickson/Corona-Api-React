import React from 'react';
import {Form} from './components'

export default (props) => {
    return(
        <>
            <div className="d-flex flex-column flex-fill bg-danger">
                <Form/>
            </div>
        </>
        )
    }

